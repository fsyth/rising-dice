#pragma once

// Display the decision network that the solver uses.
void displaySolverResults();

// Display the decision network that the solver uses in HTML.
void displaySolverResultsHtml();

// Display the decision network that the solver uses in JSON.
void displaySolverResultsJson();
